import { getCurrentUser, json } from "../../_shared/auth.js";
export async function onRequestGet({request,env}){const user=await getCurrentUser(request,env);if(!user)return json({ok:false,error:"Not authenticated"},{status:401});const q=(new URL(request.url).searchParams.get("q")||"").trim().slice(0,50),like=`%${q}%`;const r=await env.DB.prepare(`SELECT s.id,s.name,s.description,s.icon_text,s.icon_color,s.icon_data,s.created_at,(SELECT COUNT(*) FROM server_members sm WHERE sm.server_id=s.id) member_count,EXISTS(SELECT 1 FROM server_members mine WHERE mine.server_id=s.id AND mine.user_id=?) joined FROM servers s WHERE s.is_public=1 AND (?='' OR s.name LIKE ? OR s.description LIKE ?) ORDER BY member_count DESC,datetime(s.created_at) DESC LIMIT 50`).bind(user.id,q,like,like).all();return json({ok:true,servers:r.results||[]});}
